{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/target/debug/yil",
            "args": [
                "${workspaceFolder}/example-codes/sum.yil"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}